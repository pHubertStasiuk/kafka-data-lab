[mysqld]
server-id                = 1
log_bin                  = mysql-bin
binlog_format            = ROW
binlog_row_image         = FULL
expire_logs_days         = 10

binlog_cache_size        = 32M
gtid_mode                = ON
enforce_gtid_consistency = ON
performance_schema       = ON